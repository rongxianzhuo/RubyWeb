// 后端API配置
const API_CONFIG = {
    // 你的后端接口地址
    baseUrl: 'http://127.0.0.1:5000/chat',
    
    // 是否使用模拟数据（false = 使用真实后端接口）
    mockMode: false,
    
    // 请求超时时间（毫秒）
    timeout: 60000,
    
    // 是否显示调试信息
    debug: true,
    
    // 用户名称（发送给后端的 name 字段）
    userName: 'user'
};
